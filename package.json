{
  "name": "learning-by-doing",
  "private": true,
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "dev:web": "npm run dev --workspace apps/web",
    "dev:admin": "npm run dev --workspace apps/admin",
    "build:web": "npm run build --workspace apps/web",
    "build:admin": "npm run build --workspace apps/admin",
    "typecheck": "npm run typecheck --workspace packages/content-schema && npm run typecheck --workspace packages/visualization-engine && npm run typecheck --workspace packages/shared-types",
    "test": "npm run test --workspace packages/content-schema && npm run test --workspace packages/shared-types"
  },
  "devDependencies": {
    "typescript": "6.0.2",
    "vitest": "^4.0.15"
  }
}
